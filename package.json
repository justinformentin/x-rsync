{
  "name": "sftp-sync",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "sftp-sync": "dist/cli.js"
  },
  "scripts": {
    "build": "esbuild src/cli.ts --bundle --platform=node --format=cjs --outfile=dist/cli.cjs --external:cpu-features --external:*.node"
  },
  "dependencies": {
    "ssh2-sftp-client": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ssh2-sftp-client": "^9.0.6",
    "dotenv": "^17.2.3",
    "esbuild": "^0.27.0",
    "typescript": "^5.6.0"
  }
}
